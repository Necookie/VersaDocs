import { ResumeForm } from "@/components/resume-form"

export default function EditorPage() {

    return (
        <main>
            <section>
                <h1>Resume Editor Page</h1>
                <ResumeForm />
            </section>
        </main>
    )
}